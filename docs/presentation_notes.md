# Presentation Notes

## Short Demo Flow
1. Show the GitHub repository and project structure
2. Show the `src/` and `tests/` folders
3. Run automated tests
4. Run the application manually in the terminal
5. Explain how user stories connect to tests and modules

## Commands To Show
```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
.\.venv\Scripts\python.exe -m src.main
```

## Rasim's Speaking Points
- I set up the repository structure and organized the project into `src` and `tests`.
- I created and maintained the GitHub repository and ensured the project could be run locally.
- I implemented `main.py`, which acts as the start point and wires the modules together.
- I helped make the project easier to work on in parallel by using GitHub branches, commits, and pull requests.
- I will present the overall architecture and explain how the project is structured.
